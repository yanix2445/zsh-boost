# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                                 BACKUP                                       ║
# ╠══════════════════════════════════════════════════════════════════════════════╣
# ║  Crée une copie de sauvegarde horodatée                                      ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

bak() {
    if [ -z "$1" ]; then
        echo "❌ Usage: bak <fichier>"
        return 1
    fi
    
    local file=$1
    local timestamp=$(date +%Y%m%d_%H%M%S)
    local backup="${file}.bak.${timestamp}"
    
    cp -r "$file" "$backup" && gum style --foreground 82 "🔒 Backup créé : $backup"
}
