# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║                                 SERVE                                        ║
# ╠══════════════════════════════════════════════════════════════════════════════╣
# ║  Lance un serveur web statique instantané                                    ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

serve() {
    local port="${1:-8000}"
    
    # Récupère l'IP locale pour l'affichage
    local ip=$(ipconfig getifaddr en0 2>/dev/null || ipconfig getifaddr en1 2>/dev/null || echo "localhost")
    
    echo ""
    gum style --foreground 212 --border-foreground 212 --border double \
        --align center --width 50 --padding "1" \
        "🌐 SERVEUR WEB" "http://$ip:$port"
    echo ""

    python3 -m http.server "$port"
}
